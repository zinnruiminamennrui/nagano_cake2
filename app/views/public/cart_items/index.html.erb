<h4>ショッピングカート</h4>
<table class="table">
  <thead>
    <th>商品名</th>
    <th>単価(税込)</th>
    <th>数量</th>
    <th>小計</th>
    <th></th>
  </thead>
  <tbody>
    <% @cart_items.each do |cart_item| %>
    <td>
      <%= attachment_image_tag(cart_item.product, :image, size:"50x40", fallback:"no_image") %>
      <%= cart_item.product.name %>
    </td>
    <% end %>
  </tbody>
</table>